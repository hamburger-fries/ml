---
interface Props {
  imgUrl: string;
  alt: string;
  sizes: string;
  class?: string;
}

const { imgUrl, alt, sizes, class: className } = Astro.props;
---

<img
  src={imgUrl}
  alt={alt}
  sizes={sizes || '(max-width: 1200px) 100vw, 1200px'}
  class={className}
  loading="lazy"
/>
